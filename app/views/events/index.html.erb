<% content_for :pre_container do -%>
<%= render 'static_pages/parallax_header', text: 'Land Events', img_url: @background_image -%>
<% end -%>

<div class="row">
	<%= render 'layouts/featured_images', items: @events.future, heading: :title -%>
</div>

<%= render(@events.future) || '<div class="h3 text-center"><em>No events at this time.</em></div>'.html_safe %>
